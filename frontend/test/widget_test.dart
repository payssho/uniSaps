// Ce fichier est un placeholder. Les tests unitaires de l'app
// se trouvent dans test/models/, test/services/ et test/providers/.
// Les tests de widgets nécessitent un émulateur et ne sont pas couverts ici.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder – les vrais tests sont dans models/, services/ et providers/', () {
    expect(true, true);
  });
}
