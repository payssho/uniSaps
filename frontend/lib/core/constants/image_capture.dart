/// Paramètres de capture / compression à l'import photo (ImagePicker).
class ImageCaptureDefaults {
  ImageCaptureDefaults._();

  /// Photos outfit du jour, souvenirs, référence création outfit.
  static const double outfitPhotoMaxWidth = 1920;
  static const int outfitPhotoQuality = 90;

  /// Profil, onboarding (plus léger acceptable).
  static const double profilePhotoMaxWidth = 1200;
  static const int profilePhotoQuality = 88;
}
