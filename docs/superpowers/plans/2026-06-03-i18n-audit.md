# Audit i18n uniSaps — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Couvrir 100 % des textes système uniSaps en FR/EN via câblage l10n, codes d’erreur API stables et garde-fous CI.

**Architecture:** Inventaire automatisé (`gen_arb.py` comme source de vérité) → câblage des 566 clés existantes dans ~20 fichiers Dart → nouvelles clés si manquantes → backend `error_code` JSON → mapping Flutter `api_error_l10n.dart` → script CI anti-strings en dur.

**Tech Stack:** Flutter 3.24, `flutter gen-l10n`, Riverpod, FastAPI, Python 3, GitHub Actions

**Spec:** `docs/superpowers/specs/2026-06-03-i18n-audit-design.md`

---

## File map

| File | Responsibility |
|------|----------------|
| `frontend/tool/gen_arb.py` | Catalogue FR/EN (source de vérité) |
| `frontend/tool/i18n_audit.py` | Rapport d’écarts strings ↔ clés ARB |
| `frontend/tool/check_hardcoded_strings.py` | CI : échoue sur nouvelles strings UI en dur |
| `frontend/tool/extract_strings.py` | Extraction basique (réparé) |
| `frontend/lib/l10n/api_error_l10n.dart` | `error_code` → libellé localisé |
| `frontend/lib/l10n/domain_l10n.dart` | Enums / labels dynamiques (existant, étendu) |
| `frontend/lib/services/api_service.dart` | Parse `{ "error_code": "..." }` |
| `backend/app/core/errors.py` | Helper `api_error(status, code)` |
| `backend/app/api/routes/*.py` | Migration HTTPException → codes |
| `frontend/test/l10n/api_error_l10n_test.dart` | TDD mapping erreurs |
| `.github/workflows/flutter_tests.yml` | Ajout étape check i18n |

**Écrans/widgets à câbler (texte en dur détecté) :**
`dressing_screen.dart`, `dressing_category_filters_bar.dart`, `dressing_category_chips_row.dart`,
`inspiration_screen.dart`, `search_users_screen.dart`, `user_profile_screen.dart`,
`profile_screen.dart`, `outfits_screen.dart`, `creation_screen.dart`,
`creator_dressing_screen.dart`, `creator_posts_screen.dart`, `creator_profile_screen.dart`,
`creator_post_create_sheet.dart`, `creator_onboarding_screen.dart`, `creator_home_screen.dart`,
`outfit_detail_sheet.dart`, `nav_profile_bubble.dart`, `home_screen.dart`, `color_selector.dart`,
plus services : `api_service.dart`, `garment_provider.dart`, `auth_service.dart`, `weather_service.dart`.

---

## Phase 1 — Tooling & inventaire

### Task 1: Réparer extract_strings.py

**Files:**
- Modify: `frontend/tool/extract_strings.py`

- [ ] **Step 1: Corriger les regex (bug Windows / quotes)**

Remplacer les patterns avec guillemets simples dans les raw strings par des doubles quotes :

```python
PATTERNS = [
    re.compile(r'Text\s*\(\s*"([^"\\]*(?:\\.[^"\\]*)*)"'),
    re.compile(r"Text\s*\(\s*'([^'\\]*(?:\\.[^'\\]*)*)'"),
    re.compile(r'hintText:\s*"([^"]+)"'),
    re.compile(r"hintText:\s*'([^']+)'"),
    re.compile(r'label:\s*"([^"]+)"'),
    re.compile(r"label:\s*'([^']+)'"),
    re.compile(r'tooltip:\s*"([^"]+)"'),
    re.compile(r"tooltip:\s*'([^']+)'"),
]
```

- [ ] **Step 2: Vérifier l’exécution**

Run: `cd frontend; python tool/extract_strings.py`  
Expected: `Found N unique strings` (N > 100), pas de SyntaxError.

- [ ] **Step 3: Commit**

```bash
git add frontend/tool/extract_strings.py
git commit -m "fix(i18n): repair extract_strings.py for Windows"
```

---

### Task 2: Script d’audit i18n_audit.py

**Files:**
- Create: `frontend/tool/i18n_audit.py`
- Create: `frontend/tool/i18n_audit_report.json` (généré, gitignored ou commité une fois)

- [ ] **Step 1: Créer le script**

```python
#!/usr/bin/env python3
"""Compare hardcoded UI strings against gen_arb CATALOG."""
import importlib.util
import json
import re
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
LIB = ROOT / "lib"
GEN = Path(__file__).resolve().parent / "gen_arb.py"

spec = importlib.util.spec_from_file_location("gen_arb", GEN)
gen_arb = importlib.util.module_from_spec(spec)
spec.loader.exec_module(gen_arb)
CATALOG = gen_arb.CATALOG

fr_values = {v[0]: k for k, v in CATALOG.items()}
en_values = {v[1]: k for k, v in CATALOG.items()}

PATTERNS = [
    re.compile(r"Text\s*\(\s*['\"]([^'\"\\]{2,})['\"]"),
    re.compile(r"hintText:\s*['\"]([^'\"\\]{2,})['\"]"),
    re.compile(r"tooltip:\s*['\"]([^'\"\\]{2,})['\"]"),
    re.compile(r"label:\s*['\"]([^'\"\\]{2,})['\"]"),
    re.compile(r"SnackBar\s*\([^)]*content:\s*Text\s*\(\s*['\"]([^'\"\\]{2,})['\"]"),
    re.compile(r"throw Exception\s*\(\s*['\"]([^'\"\\]{2,})['\"]"),
]

SKIP = re.compile(r"^[a-z_]+$|^\d+$|^http")

entries = []
for path in sorted(LIB.rglob("*.dart")):
    if "generated" in path.parts or path.name == "l10n_context.dart":
        continue
    rel = path.relative_to(ROOT)
    for i, line in enumerate(path.read_text(encoding="utf-8").splitlines(), 1):
        if ".l10n." in line or "AppLocalizations" in line:
            continue
        for pat in PATTERNS:
            for m in pat.finditer(line):
                text = m.group(1)
                if SKIP.search(text):
                    continue
                key = fr_values.get(text) or en_values.get(text)
                entries.append({
                    "file": str(rel).replace("\\", "/"),
                    "line": i,
                    "text": text,
                    "existing_key": key,
                    "lot": 1 if key else 2,
                })

report = {
    "total": len(entries),
    "lot1_existing_key": sum(1 for e in entries if e["lot"] == 1),
    "lot2_new_key": sum(1 for e in entries if e["lot"] == 2),
    "entries": entries,
}
out = ROOT / "tool" / "i18n_audit_report.json"
out.write_text(json.dumps(report, ensure_ascii=False, indent=2), encoding="utf-8")
print(f"Wrote {out}: {report['total']} issues (lot1={report['lot1_existing_key']}, lot2={report['lot2_new_key']})")
```

- [ ] **Step 2: Exécuter l’audit**

Run: `cd frontend; python tool/i18n_audit.py`  
Expected: fichier `frontend/tool/i18n_audit_report.json` créé.

- [ ] **Step 3: Commit**

```bash
git add frontend/tool/i18n_audit.py frontend/tool/i18n_audit_report.json
git commit -m "feat(i18n): add automated audit script and baseline report"
```

---

### Task 3: Script CI check_hardcoded_strings.py

**Files:**
- Create: `frontend/tool/check_hardcoded_strings.py`

- [ ] **Step 1: Créer le garde-fou**

```python
#!/usr/bin/env python3
"""Fail if new accented/French UI literals appear outside allowlist."""
import re
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent / "lib"
ALLOWLIST = {
    "lib/core/constants/categories.dart",  # clés techniques
    "lib/l10n/domain_l10n.dart",           # mapping FR legacy DB
}
ACCENT = re.compile(r"[àâäéèêëïîôùûüçÀÂÄÉÈÊËÏÎÔÙÛÜÇ]")
PAT = re.compile(
    r"(?:Text|hintText|tooltip|label)\s*\(?\s*['\"]([^'\"]{3,})['\"]"
)

violations = []
for path in sorted(ROOT.rglob("*.dart")):
    rel = path.relative_to(ROOT.parent).as_posix()
    if "generated/" in rel or rel in ALLOWLIST:
        continue
    for i, line in enumerate(path.read_text(encoding="utf-8").splitlines(), 1):
        if ".l10n." in line:
            continue
        for m in PAT.finditer(line):
            if ACCENT.search(m.group(1)):
                violations.append(f"{rel}:{i}: {m.group(1)[:60]}")

if violations:
    print("Hardcoded French/accented UI strings found:")
    print("\n".join(violations[:50]))
    if len(violations) > 50:
        print(f"... and {len(violations) - 50} more")
    sys.exit(1)
print("i18n check passed")
```

- [ ] **Step 2: Ajouter l’étape CI**

Modify `.github/workflows/flutter_tests.yml` — insérer avant `Run tests` :

```yaml
      - name: Vérifier strings i18n (pas de FR en dur)
        run: python tool/check_hardcoded_strings.py
        working-directory: frontend
```

- [ ] **Step 3: Commit**

```bash
git add frontend/tool/check_hardcoded_strings.py .github/workflows/flutter_tests.yml
git commit -m "ci(i18n): fail on hardcoded accented UI strings"
```

> **Note:** Le check échouera tant que Phase 2 n’est pas terminée. Option : commit du script sans l’activer en CI jusqu’à Task 12, ou `--baseline` mode. **Recommandation :** activer CI en **dernière task** (Task 12).

---

## Phase 2 — Backend error codes

### Task 4: Helper errors.py + tests pytest

**Files:**
- Create: `backend/app/core/errors.py`
- Create: `backend/tests/test_errors.py`
- Create: `backend/tests/conftest.py` (vide si besoin)
- Modify: `backend/requirements.txt` (ajouter `pytest` si absent)

- [ ] **Step 1: Écrire le test**

```python
# backend/tests/test_errors.py
from app.core.errors import api_error

def test_api_error_returns_error_code_dict():
    exc = api_error(404, "garment_not_found")
    assert exc.status_code == 404
    assert exc.detail == {"error_code": "garment_not_found"}
```

- [ ] **Step 2: Run test — doit échouer**

Run: `cd backend; python -m pytest tests/test_errors.py -v`  
Expected: FAIL `ModuleNotFoundError` ou `cannot import api_error`

- [ ] **Step 3: Implémenter**

```python
# backend/app/core/errors.py
from fastapi import HTTPException

def api_error(status_code: int, code: str) -> HTTPException:
    return HTTPException(status_code=status_code, detail={"error_code": code})
```

- [ ] **Step 4: Run test — doit passer**

Run: `cd backend; python -m pytest tests/test_errors.py -v`  
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/core/errors.py backend/tests/
git commit -m "feat(api): add api_error helper with error_code payload"
```

---

### Task 5: Table des codes + migration routes backend

**Files:**
- Modify: `backend/app/core/security.py`
- Modify: `backend/app/api/routes/garments.py`
- Modify: `backend/app/api/routes/outfits.py`
- Modify: `backend/app/api/routes/posts.py`
- Modify: `backend/app/api/routes/users.py`
- Modify: `backend/app/api/routes/friends.py`
- Modify: `backend/app/api/routes/creator.py`
- Modify: `backend/app/api/routes/ai.py`

**Table de migration (obligatoire) :**

| Ancien message | `error_code` |
|----------------|--------------|
| Token Firebase invalide ou expire. | `invalid_firebase_token` |
| Vetement introuvable. | `garment_not_found` |
| Outfit introuvable. | `outfit_not_found` |
| Post introuvable. | `post_not_found` |
| Utilisateur introuvable. | `user_not_found` |
| Action non autorisee. | `action_not_allowed` |
| Impossible de s'ajouter soi-meme. | `cannot_add_self` |
| Demande deja envoyee. | `friend_request_already_sent` |
| Demande introuvable. | `friend_request_not_found` |
| Demande deja traitee. | `friend_request_already_handled` |
| Code d'activation invalide. | `invalid_activation_code` |
| Fichier vide. | `empty_file` |

- [ ] **Step 1: Migrer security.py**

```python
from .errors import api_error

# remplacer raise HTTPException(... detail="Token Firebase...") par:
raise api_error(status.HTTP_401_UNAUTHORIZED, "invalid_firebase_token")
```

- [ ] **Step 2: Migrer garments.py, outfits.py, posts.py, users.py, friends.py, creator.py, ai.py**

Exemple :

```python
from app.core.errors import api_error

raise api_error(404, "garment_not_found")
```

- [ ] **Step 3: Vérifier manuellement une route**

Run backend, curl une ressource inexistante :  
Expected body: `{"detail":{"error_code":"garment_not_found"}}`

- [ ] **Step 4: Commit**

```bash
git add backend/app/
git commit -m "refactor(api): replace French HTTPException strings with error_code"
```

> **upload.py :** conserver messages techniques en `detail` string pour erreurs infra (Firebase/GCS) ; mapper côté Flutter via codes dédiés si possible (`storage_permission_denied`, `firebase_config_missing`). Ajouter au besoin dans Task 6.

---

## Phase 3 — Couche erreurs Flutter

### Task 6: Clés l10n API + api_error_l10n.dart (TDD)

**Files:**
- Modify: `frontend/tool/gen_arb.py` (section API)
- Create: `frontend/lib/l10n/api_error_l10n.dart`
- Create: `frontend/test/l10n/api_error_l10n_test.dart`

- [ ] **Step 1: Ajouter clés dans gen_arb.py CATALOG**

```python
    "apiInvalidFirebaseToken": (
        "Session expirée. Reconnecte-toi.",
        "Session expired. Please sign in again.",
    ),
    "apiGarmentNotFound": ("Vêtement introuvable.", "Garment not found."),
    "apiOutfitNotFound": ("Outfit introuvable.", "Outfit not found."),
    "apiPostNotFound": ("Post introuvable.", "Post not found."),
    "apiUserNotFound": ("Utilisateur introuvable.", "User not found."),
    "apiActionNotAllowed": ("Action non autorisée.", "Action not allowed."),
    "apiCannotAddSelf": ("Impossible de t'ajouter toi-même.", "You can't add yourself."),
    "apiFriendRequestAlreadySent": (
        "Demande déjà envoyée.",
        "Request already sent.",
    ),
    "apiFriendRequestNotFound": (
        "Demande introuvable.",
        "Request not found.",
    ),
    "apiFriendRequestAlreadyHandled": (
        "Demande déjà traitée.",
        "Request already handled.",
    ),
    "apiInvalidActivationCode": (
        "Code d'activation invalide.",
        "Invalid activation code.",
    ),
    "apiEmptyFile": ("Fichier vide.", "Empty file."),
    "apiUnknownError": ("Une erreur est survenue.", "Something went wrong."),
    "apiNotAuthenticated": (
        "Utilisateur non authentifié.",
        "User not authenticated.",
    ),
    "apiConnectionError": (
        "Impossible de contacter le serveur.",
        "Could not reach the server.",
    ),
    "apiUploadTimeout": (
        "Le traitement de l'image prend trop de temps.",
        "Image processing is taking too long.",
    ),
```

- [ ] **Step 2: Regénérer l10n**

Run:
```bash
cd frontend
python tool/gen_arb.py
flutter gen-l10n
```

- [ ] **Step 3: Écrire le test**

```dart
// frontend/test/l10n/api_error_l10n_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:unisaps/l10n/api_error_l10n.dart';
import 'package:unisaps/l10n/generated/app_localizations_en.dart';
import 'package:unisaps/l10n/generated/app_localizations_fr.dart';

void main() {
  test('maps known error codes in FR and EN', () {
    final fr = AppLocalizationsFr('fr');
    final en = AppLocalizationsEn('en');
    expect(apiErrorL10n(fr, 'garment_not_found'), fr.apiGarmentNotFound);
    expect(apiErrorL10n(en, 'garment_not_found'), en.apiGarmentNotFound);
    expect(apiErrorL10n(fr, 'unknown_code'), fr.apiUnknownError);
  });
}
```

- [ ] **Step 4: Run test — FAIL**

Run: `cd frontend; flutter test test/l10n/api_error_l10n_test.dart`  
Expected: FAIL (fichier / fonction manquants)

- [ ] **Step 5: Implémenter api_error_l10n.dart**

```dart
import 'generated/app_localizations.dart';

String apiErrorL10n(AppLocalizations l, String code) {
  switch (code) {
    case 'invalid_firebase_token':
      return l.apiInvalidFirebaseToken;
    case 'garment_not_found':
      return l.apiGarmentNotFound;
    case 'outfit_not_found':
      return l.apiOutfitNotFound;
    case 'post_not_found':
      return l.apiPostNotFound;
    case 'user_not_found':
      return l.apiUserNotFound;
    case 'action_not_allowed':
      return l.apiActionNotAllowed;
    case 'cannot_add_self':
      return l.apiCannotAddSelf;
    case 'friend_request_already_sent':
      return l.apiFriendRequestAlreadySent;
    case 'friend_request_not_found':
      return l.apiFriendRequestNotFound;
    case 'friend_request_already_handled':
      return l.apiFriendRequestAlreadyHandled;
    case 'invalid_activation_code':
      return l.apiInvalidActivationCode;
    case 'empty_file':
      return l.apiEmptyFile;
    default:
      return l.apiUnknownError;
  }
}

/// Parse Exception / JSON backend detail → code ou null.
String? parseApiErrorCode(Object error) {
  final s = error.toString();
  final match = RegExp(r'"error_code"\s*:\s*"([a-z0-9_]+)"').firstMatch(s);
  return match?.group(1);
}
```

- [ ] **Step 6: Run test — PASS**

Run: `cd frontend; flutter test test/l10n/api_error_l10n_test.dart`  
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add frontend/tool/gen_arb.py frontend/lib/l10n/ frontend/test/l10n/
git commit -m "feat(i18n): API error codes mapping with l10n"
```

---

### Task 7: Refactor api_service.dart + add_garment_sheet.dart

**Files:**
- Modify: `frontend/lib/services/api_service.dart`
- Modify: `frontend/lib/widgets/add_garment_sheet.dart`

- [ ] **Step 1: Ajouter helper parse dans api_service**

Remplacer les `throw Exception('Erreur API...')` par des exceptions portant le code :

```dart
import '../l10n/api_error_l10n.dart';

class ApiException implements Exception {
  final String? errorCode;
  final String rawMessage;
  ApiException(this.rawMessage, {this.errorCode});
  @override
  String toString() => rawMessage;
}

String? _errorCodeFromBody(String body) {
  try {
    final data = jsonDecode(body);
    if (data is Map && data['detail'] is Map) {
      return data['detail']['error_code'] as String?;
    }
  } catch (_) {}
  return parseApiErrorCode(body);
}
```

Utiliser `ApiException(message, errorCode: code)` au lieu de `Exception(detail FR)`.

- [ ] **Step 2: Simplifier add_garment_sheet error block**

Remplacer le bloc `error.contains('connexion')` (~30 lignes) par :

```dart
import '../l10n/api_error_l10n.dart';

// dans catch / error handling:
final l = context.l10n;
String errorMessage = l.garmentSaveError;
if (errorState.hasError) {
  final err = errorState.error!;
  final code = err is ApiException ? err.errorCode : parseApiErrorCode(err);
  errorMessage = code != null ? apiErrorL10n(l, code) : l.garmentSaveError;
}
```

Conserver les clés `garmentUploadTimeout`, `garmentStoragePermissions` etc. uniquement si upload.py n’envoie pas encore de codes — migrer progressivement.

- [ ] **Step 3: Run tests + analyze**

Run:
```bash
cd frontend
flutter analyze --no-fatal-infos
flutter test
```

- [ ] **Step 4: Commit**

```bash
git add frontend/lib/services/api_service.dart frontend/lib/widgets/add_garment_sheet.dart
git commit -m "refactor(i18n): parse API error_code in api_service"
```

---

## Phase 4 — Câblage UI (Lot 1 : clés existantes)

> Pour chaque fichier : importer `l10n_context.dart`, remplacer string en dur par `context.l10n.xxx` d’après `i18n_audit_report.json` (`existing_key`). Exemple dressing :

### Task 8: dressing_screen.dart + widgets dressing

**Files:**
- Modify: `frontend/lib/screens/dressing/dressing_screen.dart`
- Modify: `frontend/lib/widgets/dressing_category_filters_bar.dart`
- Modify: `frontend/lib/widgets/dressing_category_chips_row.dart`

- [ ] **Step 1: dressing_screen.dart — remplacements**

| Texte en dur | Clé existante |
|--------------|---------------|
| `'Mon Dressing'` | `dressingMyWardrobe` |
| `'Aucun résultat'` | `dressingNoResults` |
| `'Aucun vêtement'` | `dressingNoGarments` |
| `'Essaie un autre nom...'` | `dressingTryOtherFilters` |
| `'Ajoute ton premier vêtement !'` | `dressingAddFirstGarment` |
| `'Ajouter un vêtement'` | `dressingAddGarment` |
| `'Ajouter'` | `dressingAddShort` |

Ajouter en tête : `import '../../l10n/l10n_context.dart';`

- [ ] **Step 2: Appliquer même pattern aux widgets dressing**

- [ ] **Step 3: Vérifier EN**

Run app → Réglages → English → écran Dressing : titres en anglais.

- [ ] **Step 4: Commit**

```bash
git add frontend/lib/screens/dressing/ frontend/lib/widgets/dressing_*
git commit -m "feat(i18n): wire dressing screens to l10n"
```

---

### Task 9: inspiration_screen.dart + search + user_profile

**Files:**
- Modify: `frontend/lib/screens/inspiration/inspiration_screen.dart`
- Modify: `frontend/lib/screens/inspiration/search_users_screen.dart`
- Modify: `frontend/lib/screens/inspiration/user_profile_screen.dart`

- [ ] **Step 1: Utiliser le rapport d’audit pour mapper clés existantes**

Run: `python tool/i18n_audit.py` et filtrer `"file": "lib/screens/inspiration/..."`.

Clés probables déjà dans gen_arb : `navInspiration`, `inspiration*`, `userProfile*`, `commonSearch`, etc.

- [ ] **Step 2: Remplacer toutes les strings Lot 1**

- [ ] **Step 3: Lot 2 — ajouter clés manquantes dans gen_arb.py, regénérer**

Run:
```bash
python tool/gen_arb.py && flutter gen-l10n
```

- [ ] **Step 4: Commit**

```bash
git add frontend/lib/screens/inspiration/ frontend/tool/gen_arb.py frontend/lib/l10n/
git commit -m "feat(i18n): wire inspiration module to l10n"
```

---

### Task 10: profile_screen.dart + nav_profile_bubble + home

**Files:**
- Modify: `frontend/lib/screens/profile/profile_screen.dart`
- Modify: `frontend/lib/widgets/nav_profile_bubble.dart`
- Modify: `frontend/lib/screens/home/home_screen.dart`

- [ ] **Step 1: Câbler via clés `profile*`, `navProfile`, clés existantes**

- [ ] **Step 2: `nav_profile_bubble.dart` — `'Profil'` → `context.l10n.navProfile`**

- [ ] **Step 3: Vérifier parcours Profil EN**

- [ ] **Step 4: Commit**

```bash
git add frontend/lib/screens/profile/ frontend/lib/widgets/nav_profile_bubble.dart frontend/lib/screens/home/
git commit -m "feat(i18n): wire profile and nav to l10n"
```

---

### Task 11: Module creator (5 écrans)

**Files:**
- Modify: `frontend/lib/screens/creator/creator_dressing_screen.dart`
- Modify: `frontend/lib/screens/creator/creator_posts_screen.dart`
- Modify: `frontend/lib/screens/creator/creator_profile_screen.dart`
- Modify: `frontend/lib/screens/creator/creator_post_create_sheet.dart`
- Modify: `frontend/lib/screens/creator/creator_onboarding_screen.dart`
- Modify: `frontend/lib/screens/creator/creator_home_screen.dart`

- [ ] **Step 1: Mapper avec clés `creator*`, `creatorTab*`, `creatorLanding*`**

- [ ] **Step 2: Lot 2 — nouvelles clés si audit les liste**

- [ ] **Step 3: Commit**

```bash
git add frontend/lib/screens/creator/ frontend/tool/gen_arb.py frontend/lib/l10n/
git commit -m "feat(i18n): wire creator module to l10n"
```

---

### Task 12: outfits + creations + widgets restants + services

**Files:**
- Modify: `frontend/lib/screens/outfits/outfits_screen.dart`
- Modify: `frontend/lib/screens/creations/creation_screen.dart`
- Modify: `frontend/lib/widgets/outfit_detail_sheet.dart`
- Modify: `frontend/lib/widgets/color_selector.dart`
- Modify: `frontend/lib/services/auth_service.dart`
- Modify: `frontend/lib/services/weather_service.dart`
- Modify: `frontend/lib/providers/garment_provider.dart`

- [ ] **Step 1: Câbler écrans outfits/creations (clés `outfits*`, `creation*`)**

- [ ] **Step 2: Remplacer `throw Exception('...')` FR dans services par `ApiException` + codes ou clés l10n au point d’affichage**

- [ ] **Step 3: Run audit — objectif lot2 = 0**

Run: `python tool/i18n_audit.py`  
Expected: `lot2_new_key: 0` (ou proche)

- [ ] **Step 4: Activer CI check (Task 3) si pas encore fait**

- [ ] **Step 5: Run check**

Run: `python tool/check_hardcoded_strings.py`  
Expected: `i18n check passed`

- [ ] **Step 6: Commit**

```bash
git add frontend/
git commit -m "feat(i18n): complete UI and service string migration"
```

---

## Phase 5 — Vérification finale

### Task 13: Tests & parcours manuel

- [ ] **Step 1: Suite de tests**

Run:
```bash
cd frontend && flutter test --reporter github
cd ../backend && python -m pytest tests/ -v
```

Expected: all PASS

- [ ] **Step 2: Parcours manuel FR**

Checklist : Auth → Dressing → Outfits → Inspiration → Profil → Créateur → changement langue EN → répéter.

- [ ] **Step 3: Tester erreurs API**

Déconnecter token / accès garment inexistant → message EN cohérent, pas de FR backend brut.

- [ ] **Step 4: Regénérer rapport final**

Run: `python tool/i18n_audit.py` — archiver dans commit si utile.

- [ ] **Step 5: Commit final docs**

```bash
git add docs/superpowers/ frontend/tool/i18n_audit_report.json
git commit -m "docs(i18n): finalize audit report and verification"
```

---

## Self-review (spec coverage)

| Exigence spec | Task |
|---------------|------|
| Audit automatisé | 1, 2 |
| gen_arb source de vérité | 6, 9–12 |
| Câblage clés existantes | 8–12 |
| error_code backend | 4, 5 |
| Mapping Flutter API | 6, 7 |
| domain_l10n étendu | 12 (si enums restants) |
| CI anti-régression | 3, 12 |
| UGC non traduit | Hors tâches (ne pas traduire bios/légendes) |
| Vérification | 13 |

---

## Ordre d’exécution recommandé

```
Task 1 → 2 → 4 → 5 → 6 → 7 → 8 → 9 → 10 → 11 → 12 → 3 (CI) → 13
```

Backend (4–5) en parallèle possible avec tooling (1–2) si deux agents.

---

## Estimation

| Phase | Durée |
|-------|-------|
| 1 Tooling | 2–3 h |
| 2 Backend | 2–3 h |
| 3 Flutter API | 2 h |
| 4 UI câblage | 6–10 h |
| 5 Vérification | 1–2 h |

**Total : ~2–3 jours**
